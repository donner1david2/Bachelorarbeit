\chapter{Server} \label{sec:server}

Für das Speichern und Bereitstellen der Videos wird ein Server benötigt, der die empfangenen Videodaten und deren Metadaten speichert und auf Anfrage eines Nutzers bereitstellt. Dieser muss dabei in der Lage sein, eine größere Anzahl an Nutzern gleichzeitig zu bedienen.

Die Serveranwendung wurde in Kotlin auf Grundlage von Spring Boot implementiert. Sie stellt die benötigten HTTP-Endpunkte für den Upload und den Abruf der Videodaten bereit und übernimmt die Verwaltung der zugehörigen Metadaten.

\section{Wahl des Servertyps}

Für den Server wurde entschieden, auf einen klassischen Webserver kombiniert mit relationaler Datenbank zurückzugreifen, der die Videos auf dem Dateisystem speichert. Ein Alternativer Gedanke wäre es, statt das Dateisystem des Servers zu verwenden, die Videos in einem Object-Storage zu speichern. 
Dies ist mittlerweile eine empfohlene Praxis bei \ac{VOD} Streaming Plattformen \cite{2021}. Object-Storage bringt jedoch viel Komplexität mit sich, die nicht gerechtfertigt werden kann. Denn die relativ geringe Menge an Videos mit denen zu rechnen ist, 
vermindern stark die Vorteile die Object-Storage mit sich bringt. Deshalb wurde entschieden, die Videos auf dem Dateisystem des Servers zu speichern. Da die Videos maximal eine Länge von einigen Minuten haben, ist die Dateigröße relativ klein, 
sodass eine Speicherung auf dem Dateisystem des Servers keine Probleme mit sich bringen sollte, und die Verknüpfung von Metadaten und Videos stark vereinfacht wird.

Als Kommunikationsprotokoll zwischen Server und Clients wird HTTP zusammen mit REST-API verwendet. Da dies eine simple aber ausreichende Methode ist. Der größte Nachteil ist das abgebrochene Anfragen nicht fortgesetzt werden können. Somit kann die Übertragung von großen Dateien problematisch werden.
Da aber HLS als Streaming Protokoll verwendet wird, (s. \ref{sec:hls}) liegen die Videos nicht als eine einzelne große Datei vor, sondern eine Playlist Datei mit einzelnen Video Segmenten, welche nicht größer als 5MB werden. Deshalb ist die Übertagung der Videos kein Problem.
Es braucht deutlich reduzierten Aufwand, einen REST-Server mittels Spring Boot aufzusetzen, als bei einer vergleichbaren Implementierung ohne den Einsatz eines Frameworks. Spring Boot übernimmt den Großteil der Aufgaben und es müssen nur die Endpunkte für die Clients, 
die Schnittstellen zur Datenbank und wie erhaltene Anfragen bearbeitet werden müssen definiert werden. Da eine große Menge strukturierter Video-Metadaten gespeichert werden, wurde eine relationale Datenbank zur effizienten Verwaltung dieser Daten eingesetzt.

\subsection{Speicherkonzept für Videodaten} 

Für die Speicherung der Videodaten wurde im Prototyp eine Trennung zwischen den eigentlichen Videodateien und den dazugehörigen Metadaten umgesetzt. Die Videodateien werden dabei im Dateisystem des Servers abgelegt, während die beschreibenden Informationen zu einem Video 
in einer relationalen Datenbank gespeichert werden. Diese Aufteilung entspricht dem grundlegenden Aufbau der Serverkomponente, bei dem der Server sowohl Zugriff auf das Dateisystem als auch auf eine PostgreSQL-Datenbank besitzt. 

Der wesentliche Grund für diese Trennung liegt in den unterschiedlichen Eigenschaften der gespeicherten Daten. Videodateien bestehen aus vergleichsweise großen binären Datenmengen, die nach dem Upload im Regelfall vollständig abgelegt und später wieder gelesen werden. 
Für solche großen Objekte ist eine Speicherung als eigenständige Datei im Dateisystem geeignet, da Dateisysteme für das sequenzielle Lesen und Schreiben größerer Dateien ausgelegt sind. Untersuchungen zur Speicherung großer Objekte zeigen, 
dass Datenbanken insbesondere bei kleineren Objekten Vorteile haben können, während Dateisysteme bei größeren Objekten häufig effizienter sind \cite{sears_van_ingen_gray_2006}. Da die erzeugten Videos nicht als kleine Datensätze, 
sondern als zusammenhängende Mediendateien beziehungsweise als HLS-Playlist mit Segmentdateien vorliegen, ist eine Speicherung außerhalb der Datenbank für diesen Anwendungsfall naheliegend. 

Die relationale Datenbank übernimmt dagegen die Verwaltung der strukturierten Informationen zu den Videos. Dazu gehören beispielsweise: 

\begin{itemize} 
    \item die Spieler-ID, 
    \item der Dateiname, 
    \item der Speicherort, 
    \item der Erstellungszeitpunkt und 
    \item der aktuelle Status des Videos. 
\end{itemize} 

Diese Daten müssen gezielt durchsucht, gefiltert und über die Webplattform angezeigt werden können. Eine relationale Datenbank eignet sich hierfür, da die Metadaten in einer festen Struktur abgelegt und über Abfragen ausgewertet werden können. Die Videodatei selbst wird in diesem Modell nicht direkt in der Datenbank gespeichert, 
sondern über einen gespeicherten Dateipfad mit dem Datenbankeintrag verknüpft. Dadurch entsteht eine eindeutige Beziehung zwischen dem physischen Speicherort der Datei und dem logischen Eintrag im System. 

Ein Videoeintrag in der Datenbank kann somit als zentrale Referenz auf eine gespeicherte Aufnahme verstanden werden. Der Datenbankeintrag enthält die fachlichen Informationen, während der Dateipfad auf die konkrete Datei oder den Ordner mit den HLS-Segmenten verweist. 
Wird ein Video über die Webplattform gesucht, werden zuerst die Metadaten aus der Datenbank geladen. Erst beim eigentlichen Abspielen wird die über den Dateipfad referenzierte Videodatei beziehungsweise die zugehörige HLS-Playlist vom Server ausgeliefert. 
Dadurch müssen bei Such- und Listenansichten keine großen Videodaten aus der Datenbank gelesen werden. 

Ein alternativer Ansatz wäre die Speicherung der Videodaten direkt in PostgreSQL, etwa als Large Object. PostgreSQL stellt hierfür grundsätzlich Mechanismen bereit, mit denen große Datenobjekte über eine spezielle Large-Object-Struktur verwaltet werden können \cite{2026}. 
Für den vorliegenden Prototypen ergibt sich daraus jedoch kein wesentlicher Vorteil, da die Videos nicht innerhalb komplexer Datenbanktransaktionen verarbeitet werden müssen. Stattdessen genügt es, die Datei zuverlässig abzulegen und den Speicherort in der Datenbank zu speichern. 
Damit bleibt die Datenbank auf die Verwaltung strukturierter Metadaten beschränkt, während das Dateisystem die binären Videodaten verwaltet. 

Diese Trennung reduziert außerdem die Komplexität der Anwendung. Die Datenbank enthält nur kleine und gut abfragbare Datensätze, während die großen Videodateien unabhängig davon abgelegt werden. Gleichzeitig bleibt die Verbindung zwischen beiden Bestandteilen eindeutig nachvollziehbar. 
Solange der Datenbankeintrag und der Dateipfad konsistent gehalten werden, kann jedes Video einem Spieler und einem konkreten Speicherort zugeordnet werden. Für den Prototyp ist dieses Speicherkonzept ausreichend, 
da die zu erwartende Datenmenge begrenzt ist und die Videos nur eine Länge von wenigen Minuten besitzen.

\subsection{Vergleich von Dateisystem und Object Storage} 

Neben der Speicherung im Dateisystem wurde auch die Verwendung eines Object Storage betrachtet. Object Storage speichert Daten nicht als klassische Dateien in einer lokalen Ordnerstruktur, sondern als Objekte in einem Speicherbereich. 
Jedes Objekt besitzt einen eindeutigen Schlüssel und kann über eine Schnittstelle abgerufen werden. Dieser Ansatz wird häufig bei großen verteilten Speichersystemen verwendet, da er auf hohe Skalierbarkeit und die Verwaltung großer Datenmengen ausgelegt ist \cite{sears_van_ingen_gray_2006}. 

Ein wesentlicher Vorteil von Object Storage liegt in der Skalierbarkeit. Die Videodaten sind nicht an das Dateisystem eines einzelnen Servers gebunden, sondern können unabhängig von der Anwendung gespeichert werden.
Dadurch kann ein System mit mehreren Serverinstanzen einfacher auf denselben Datenbestand zugreifen. Für große produktive Videoplattformen ist dies ein wichtiger Vorteil, da Speicherplatz und Serverkapazität getrennt voneinander erweitert werden können. 
Object-Storage-Systeme bieten häufig zusätzliche Funktionen wie: 

\begin{itemize} 
    \item Replikation, 
    \item Lebenszyklus-Regeln und 
    \item Zugriffskontrollen. 
\end{itemize}

Demgegenüber steht jedoch eine höhere Komplexität. Für den Einsatz eines Object Storage muss eine zusätzliche Speicherkomponente eingebunden werden. Die Anwendung muss nicht mehr nur lokale Dateien schreiben und lesen, sondern über eine externe Schnittstelle mit dem Speichersystem kommunizieren. 
Dabei müssen Aspekte wie Authentifizierung, Berechtigungen, Bucket-Strukturen, Objektpfade und Fehlerbehandlung berücksichtigt werden. Für einen Prototypen erhöht dies den Entwicklungsaufwand deutlich. 

Auch der Betriebsaufwand ist bei Object Storage höher. Neben dem eigentlichen Server muss ein zusätzlicher Speicherdienst betrieben oder ein externer Dienstleister genutzt werden. Dabei entstehen weitere Konfigurations- und Verwaltungsaufgaben. Bei einem lokalen Dateisystem ist der Betrieb einfacher, 
da der Server direkt auf die gespeicherten Dateien zugreifen kann. Für die im Prototyp erwartete Datenmenge ist diese Einfachheit ein wichtiger Vorteil, da zunächst die grundsätzliche Funktionalität der Videoübertragung, Speicherung und Bereitstellung im Vordergrund steht. 

Ein weiterer Unterschied betrifft die Kosten. Bei einer lokalen Speicherung entstehen vor allem Kosten für den vorhandenen Server und dessen Speicherplatz. Object Storage kann dagegen nutzungsabhängige Kosten verursachen, beispielsweise für gespeicherte Daten, Speicherzugriffe und Datenübertragungen. 
Diese Kostenstruktur kann bei sehr großen Datenmengen sinnvoll sein, da Speicher flexibel erweitert werden kann. Für eine begrenzte Anzahl kurzer Videos ist dieser Vorteil jedoch weniger entscheidend. 

Auch hinsichtlich der Integration ist das Dateisystem für den Prototyp einfacher geeignet. Der Server kann die empfangenen Videodaten direkt in einem Verzeichnis speichern und den Pfad in der Datenbank ablegen. Beim Abruf kann dieser Pfad verwendet werden, 
um die HLS-Playlist und die dazugehörigen Segmente auszuliefern. Die bestehende REST-Struktur des Servers kann dadurch ohne zusätzliche Speicher-API umgesetzt werden. Bei Object Storage müsste dagegen zusätzlich entschieden werden, wie Objektschlüssel aufgebaut werden, 
wie die Segmente hochgeladen werden und wie der Zugriff auf einzelne Objekte erfolgt. 

Für den vorliegenden Prototypen und eine erste Version überwiegen daher die Vorteile des Dateisystems. Die erwartete Anzahl an Videos ist begrenzt, die Videos sind vergleichsweise kurz und durch HLS ohnehin in kleinere Dateien aufgeteilt. Die Speicherung im Dateisystem ist einfach umzusetzen, 
reduziert die Anzahl zusätzlicher Komponenten und passt zur bestehenden Serverlogik. Object Storage bleibt dennoch eine sinnvolle Option für einen späteren produktiven Ausbau. Sobald mehrere Serverinstanzen eingesetzt werden, 
sehr große Datenmengen entstehen oder höhere Anforderungen an Verfügbarkeit und Skalierbarkeit bestehen, kann ein Wechsel auf Object Storage erneut betrachtet werden.

\subsection{Verwaltung von Videometadaten} 

Damit gespeicherte Videos später gefunden, zugeordnet und bereitgestellt werden können, müssen neben den eigentlichen Videodateien auch Metadaten verwaltet werden. Diese Metadaten beschreiben ein Video, ohne selbst Bestandteil der Videodatei zu sein. 
Im entwickelten System werden sie in einer relationalen Datenbank gespeichert. Die Datenbank bildet damit die zentrale Stelle für alle Informationen, die zur Identifikation, Suche und Verwaltung der Videos benötigt werden. 

Zu den wichtigsten Metadaten gehört die Spieler-ID. Sie stellt die Verbindung zwischen einer gespeicherten Aufnahme und dem Spieler her, dem das Video zugeordnet ist. Dadurch kann die Webplattform Videos nach Spielern filtern oder einem Nutzer gezielt die zugehörigen Aufnahmen anzeigen. 
Zusätzlich wird der Dateiname gespeichert. Dieser identifiziert die konkrete Videodatei beziehungsweise die HLS-Playlist innerhalb des Speicherbereichs. Ergänzend dazu wird der Speicherort abgelegt, damit der Server die Datei oder den Ordner mit den Segmenten im Dateisystem finden kann. 

Ein weiterer relevanter Wert ist der Erstellungszeitpunkt. Dieser ermöglicht es, Videos zeitlich zu sortieren und verschiedene Aufnahmen eines Spielers voneinander zu unterscheiden. Gerade wenn ein Spieler mehrere Spiele absolviert, ist der Zeitpunkt notwendig, 
um die Videos in eine nachvollziehbare Reihenfolge zu bringen. Zusätzlich könnte ein Videostatus gespeichert werden. Dieser beschreibt, ob ein Video beispielsweise gerade hochgeladen, vollständig gespeichert, verarbeitet oder bereits für den Abruf verfügbar ist. Dadurch kann verhindert werden, 
dass ein Nutzer ein Video anfordert, das noch nicht vollständig bereitgestellt wurde. 

Die Metadaten können außerdem um weitere Spielinformationen ergänzt werden. Dazu zählen beispielsweise: 

\begin{itemize} 
    \item eine Spiel-ID, 
    \item eine Turnier-ID, 
    \item der Spielmodus und 
    \item Informationen zum Ergebnis. 
\end{itemize}

Für den Prototypen ist eine minimale Struktur ausreichend, die den Upload, die Speicherung und den Abruf ermöglicht. 
Für eine spätere stärkere Integration in das Dart-Ökosystem wäre eine Erweiterung jedoch sinnvoll. Dann könnten Videos nicht nur einem Spieler, sondern auch einem konkreten Spiel oder Wettbewerb zugeordnet werden. 

Die getrennte Speicherung von Metadaten und Videodateien hat den Vorteil, dass die Datenbank nur strukturierte und vergleichsweise kleine Datensätze enthält. Bei einer Anfrage an die Webplattform müssen dadurch nicht die großen Videodaten geladen werden. 
Stattdessen kann der Server zunächst nur die Metadaten auslesen und daraus eine Übersicht vorhandener Videos erzeugen. Erst wenn ein Nutzer ein bestimmtes Video auswählt, wird der gespeicherte Dateipfad verwendet, um die zugehörige HLS-Playlist und die Segmentdateien bereitzustellen. 

Für die Konsistenz des Systems ist wichtig, dass der Datenbankeintrag und die Videodatei zusammenpassen. Wird eine Datei erfolgreich gespeichert, muss auch der zugehörige Datenbankeintrag angelegt oder aktualisiert werden. Umgekehrt sollte ein Video nicht als verfügbar markiert werden, 
wenn die Datei im Dateisystem fehlt oder der Upload noch nicht abgeschlossen ist. Der Videostatus kann dabei genutzt werden, um diese Zustände abzubilden. Dadurch bleibt nachvollziehbar, welche Videos vollständig vorhanden sind und welche noch nicht abgerufen werden sollten. 

Die Verwaltung der Videometadaten bildet damit die Grundlage für die spätere Nutzung der Webplattform. Ohne diese Informationen könnten die gespeicherten Dateien zwar technisch vorhanden sein, wären aber fachlich nicht sinnvoll zuordenbar. Erst durch die Metadaten wird ersichtlich, 
zu welchem Spieler ein Video gehört, wann es erstellt wurde und unter welchem Speicherort es abgerufen werden kann. Deshalb werden die Videodateien im Dateisystem gespeichert, während die Metadaten getrennt davon in PostgreSQL verwaltet werden.

\subsection{Datenmodell} 

Für die Verwaltung der Videometadaten wird ein einfaches Datenmodell verwendet. Da die Videodateien selbst nicht in der Datenbank gespeichert werden, sondern im Dateisystem liegen, muss die Datenbank nur die Informationen enthalten, die zur Identifikation, 
Zuordnung und Bereitstellung eines Videos notwendig sind. Das Datenmodell besteht im Prototyp daher aus einer einzelnen Entität \textit{Video} (s. Abb. \ref{fig:video-entity}). Auch wenn dadurch nur eine Tabelle entsteht, 
bildet diese Tabelle die zentrale Verbindung zwischen den gespeicherten Videodateien und der fachlichen Nutzung über die Webplattform. 

\begin{figure}[H] 
    \centering 
    \includesvg[width=0.35\textwidth]{img/PlantUml/video-entity/video-entity.svg} 
    \caption[Video-Entität des Datenmodells]{Video-Entität zur Speicherung der Videometadaten. Die eindeutige ID identifiziert den jeweiligen Datenbankeintrag. Die Attribute name und path beschreiben die Zuordnung und den Speicherort des Videos. throws enthält die Anzahl der aufgezeichneten Würfe,
    date den Erstellungszeitpunkt und tournament die Kennung des zugehörigen Turniers. Die eigentliche Videodatei wird nicht in der Datenbank, sondern unter dem durch path referenzierten Speicherort im Dateisystem abgelegt.}
    \label{fig:video-entity} 
\end{figure} 

Die Entität besitzt eine eindeutige \textit{id}, über die jeder Datensatz innerhalb der Datenbank identifiziert werden kann. Momentan wird in \textit{name} nur die PlayerId vom Dart gespeichert da es für den Prototypen nicht notwendig war ein Benennungsschema für die Videos zu erstellen. 
In einer richtigen Implementierung sollten dann passende Namen vergeben werden, und die PlayerId kann in ein eigenes Attribut wandern. Der Wert \textit{path} beschreibt den Pfad der gespeicherten Videodatei beziehungsweise der HLS-Playlist. Ergänzend dazu enthält das Attribut \textit{throws}.
Dieser wert kann entweder als Statistik zum sortieren verwendet werden, oder im Fall das man die Videolänge angeben möchte können wir diese mit den Würfen berechnen da wir wissen, dass ein Wurf genau 3 Sekunden entspricht. 

Der Erstellungszeitpunkt wird über \textit{date} gespeichert. Dieser Wert ist notwendig, um mehrere Videos eines Spielers zeitlich einordnen und in einer nachvollziehbaren Reihenfolge anzeigen zu können. Das Attribut \textit{tournament} beschreibt das zugehörige Turnier des Videos. 
Um später den Nutzern zu ermöglichen alle Videos zu einem bestimmten Turnier anzuschauen.

Das Datenmodell bleibt damit zunächst bewusst klein gehalten. Für den Prototypen reicht diese Struktur aus, da zunächst nur der Upload, die Speicherung, die Zuordnung und der Abruf der Videos umgesetzt werden müssen. Eine spätere Erweiterung wäre dennoch möglich. 
So könnten zusätzliche Attribute wie eine Spiel-ID oder weitere Informationen zum Spiel ergänzt werden, falls die Videofunktion stärker in das bestehende Dart-Ökosystem eingebunden wird. Für die aktuelle Umsetzung ist die einzelne Tabelle jedoch ausreichend, 
da sie alle für die Serverlogik benötigten Metadaten enthält und gleichzeitig die Trennung zwischen Datenbank und Dateisystem beibehält.

\section{API-Endpunkte}

\begin{figure}[H]
    \centering
    \includesvg[width=0.6\textwidth]{img/PlantUml/RESTapi/RESTapi.svg}
    \caption[REST-API-Endpunkte des Video-Servers]{Struktur der REST-API-Endpunkte des Video-Servers. Unterhalb des Basispfads \texttt{/video} ermöglicht der Pfad \texttt{/\{playerId\}/\{filename\}} mit einer POST-Anfrage das Hochladen eines Videos durch den HB10. 
    Eine GET-Anfrage an denselben Pfad dient dem Abruf der zugehörigen HLS-Playlist oder eines Videosegments. Über \texttt{/video/videos} stellt eine GET-Anfrage die Metadaten der verfügbaren Videos für die Webplattform bereit.}
    \label{fig:rest}
\end{figure}

Für die Kommunikation zwischen dem Server und den Clients werden REST-API-Endpunkte (s. \cref{fig:rest}) bereitgestellt. Diese Endpunkte ermöglichen es den Clients, Videos hochzuladen, Metadaten abzurufen und Videos zu streamen (s. \cref{fig:videocycle}) . Die API-Endpunkte sind wie folgt strukturiert:

\begin{itemize}
    \item \textbf{POST /video/\{playerId\}/\{filename\}}: Ein Endpunkt für den HB10, um ein Video hochzuladen. Der Endpunkt erhält die spielerId den Dateinamen und die Videodaten.
    \item \textbf{GET /video/\{playerId\}/\{filename\}}: Ein Endpunkt für den Nutzer, um die HLS playlist und Segmente für ein bestimmtes Video abzurufen.
    \item \textbf{GET /video/videos}: Ein Endpunkt für den Nutzer, um eine Liste aller verfügbaren Videos abzurufen. Der Endpunkt gibt eine Liste von Metadaten über die Videos zurück.
\end{itemize}

\begin{figure}[H]
    \centering
    \includesvg[width=\textwidth]{img/PlantUml/VideoZyklus/VideoZyklus.svg}
    \caption[Lebenszyklus eines Videos]{Lebenszyklus eines Videos. Nachdem das Video auf dem HB10 fertig ist, wird es an den Server übertragen und dort auf dem Dateisystem gespeichert. Die dazugehörigen Metadaten werden in einer Datenbank gespeichert. Wird das Video jetzt vom Webclient abgerufen, 
    fragt dieser zunächst die Liste aller Videos ab und zeigt sie an. Wird ein Video ausgewählt, wird zunächst die dazugehörige HLS Playlist angefragt und danach die zugehörigen Segmente des Videos geladen.}
    \label{fig:videocycle}
\end{figure}

Für die Bereitstellung des Webclients wird ein statischer Endpunkt mittels SpringBoot bereitgestellt. 

\ac{JPA} \cite{springdatajpa_getting_started} kümmert sich um das Handling der Datenbank. Solange man eine passende Entitäts-Klasse definiert, welche bestimmt wie eine Tabelle aussieht. Kann man mit der Datenbank über Listen dieser Entitäten einfach im Code interagieren, 
und muss nicht selbst sql für die Interaktion mit der Datenbank schreiben.

\section{Wahl des Streaming Protokolls} \label{sec:hls}

Für die Auswahl eines passenden Streaming Protokolls müssen einige Faktoren beachtet werden. Die benötigte Bandbreite, um einerseits die Auslastung des Servers niedrig zu halten, und es auch Nutzern mit niedriger Download-Geschwindigkeit zu ermöglichen, die Videos anzuschauen. 
Weitere wichtige Faktoren sind die benötige Verbindungsdauer und die Verbindungsart, wenn der Server für jeden Nutzer eine Socket-Verbindung aufrecht erhalten muss, führt dies auch bei moderater Nutzung zu Problemen alle Nutzer gleichzeitig zu bedienen. 
Deshalb ist das passende Streaming Protokoll zu finden essentiell, um die \ac{VOD}-Platform Ressourcen effizient zu hosten, und dem Nutzer ein angenehmes Erlebnis zu bieten.

Im Zuge einer Recherche wurden folgende Protokolle auf die Eignung für unseren Anwendungsfall untersucht: \ac{RTMP} \parencite{lei_jiang_wang_2012} und \ac{HLS} \parencite{Saini2026}.

Für die Implementierung der \ac{VOD} Platform wird HLS verwendet. Ein wesentlicher Grund für den Einsatz von \ac{HLS} anstelle von Real-Time Messaging Protocol \ac{RTMP} liegt in der unterschiedlichen Integration in moderne Webtechnologien und deren Weiterentwicklung. 
RTMP wurde ursprünglich für den Einsatz mit dem Adobe Flash Player konzipiert, welcher heute nicht mehr unterstützt wird, wodurch die praktische Nutzbarkeit von RTMP stark eingeschränkt ist \cite{lei_jiang_wang_2012}. 
Im Gegensatz dazu basiert HLS auf standardisierten HTTP-Mechanismen und ist dadurch mit einer Vielzahl von Geräten und Browsern ohne zusätzliche Plugins kompatibel. 

Darüber hinaus ermöglicht \ac{HLS} als Vertreter von HTTP Adaptive Streaming eine dynamische Anpassung der Bitrate an die aktuellen Netzwerkbedingungen, was zu einer verbesserten Erfahrung für den Nutzer führt \cite{Saini2026}. 
Diese Eigenschaft ist besonders in heterogenen Netzumgebungen mit schwankender Bandbreite von entscheidender Bedeutung. Moderne Studien zeigen zudem, dass adaptive Streaming-Ansätze in Kombination mit aktuellen Transportprotokollen eine effiziente Nutzung von Netzwerkressourcen ermöglichen 
und die Leistungsfähigkeit gegenüber älteren, persistenten Verbindungen erhöhen können \cite{10.1145/3793674}. 

Zusammenfassend bietet HLS gegenüber RTMP entscheidende Vorteile hinsichtlich Kompatibilität, Skalierbarkeit und Anpassungsfähigkeit, weshalb es sich als de-facto-Standard für heutige Streaming-Anwendungen etabliert hat.

Alternativ zu HLS wäre auch MPEG-DASH \parencite{Saini2026} eine Option, sie basieren auf einem ähnlichen Prinzip, und für die Implementierung gäbe es wahrscheinlich keine großen Unterschiede, jedoch verwendet Apple ausschließlich HLS auf seinen Geräten.
Somit müsste für die Integration von Apple Geräten ohnehin HLS verwendet werden, zusätzlich dazu noch MPEG-DASH einzubauen würde keinen Mehrwert bringen.

\section{Integration in die LÖWEN-Dart Umgebung}

Es bestehen verschiedene Möglichkeiten, wie der Server in die Dart-Umgebung integriert werden kann. Auf der einen Seite besteht die Möglichkeit, einen eigenständigen Server aufzubauen, genau wie in diesem Kapitel beschrieben,
und anschließend alle Daten die von anderen Komponenten der Dart-Umgebung stammen, welche für die Darstellung benötigt wird, direkt vom HB10 zu erhalten, oder es dem Server erlauben mit dem Bereits bestehenden Dart-Server zu kommunizieren.
Auf der anderen Seite, besteht auch die Möglichkeit die Funktionalität in den bestehenden Dart-Server zu integrieren. Da der Dart-Server bereits über eine Angebundene PostgreSQL Datenbank, und verschiedene REST-API-Endpunkte verfügt, sollte sich diese Integration relativ simpel gestalten.
In diesem Fall müsste man jedoch untersuchen, ob der Dart-Server ohne Probleme in der Lage ist, zusätzlich zu seinen bestehenden aufgaben die Video-Übertragung zu übernehmen.

\section{Skalierbarkeit des Servers} 

Für den im Rahmen dieser Arbeit entwickelten Prototypen ist die Verwendung eines einzelnen Servers ausreichend. Die erwartete Anzahl an Nutzern und HB10-Geräten ist vergleichsweise gering, sodass die verfügbare Rechenleistung sowie die Netzwerkbandbreite eines einzelnen Systems den Anforderungen genügen. 
Aus diesem Grund wurde auf zusätzliche Maßnahmen wie Load Balancing oder geografisch verteilte Serverstandorte verzichtet. 

Sollte das System zukünftig produktiv eingesetzt werden und die Anzahl der Nutzer oder angeschlossenen HB10-Geräte deutlich steigen, könnte die Architektur schrittweise erweitert werden. Eine naheliegende Möglichkeit besteht darin, mehrere Instanzen der Serveranwendung parallel zu betreiben. 
Dabei würde ein Load Balancer die eingehenden Anfragen auf mehrere Server verteilen. Dieses Vorgehen wird als horizontale Skalierung bezeichnet und ermöglicht es, die Verarbeitungskapazität durch das Hinzufügen weiterer Server zu erhöhen, ohne die Anwendung grundlegend verändern zu müssen \cite{Al-Dulaimy2024}. 

Da im vorgestellten System die Videodateien und Metadaten über standardisierte HTTP-Schnittstellen bereitgestellt werden und die Geschäftslogik weitgehend zustandslos umgesetzt ist, eignet sich die Architektur grundsätzlich für eine solche horizontale Skalierung. Fällt eine Serverinstanz aus, 
können eingehende Anfragen weiterhin von den verbleibenden Instanzen verarbeitet werden, wodurch sich gleichzeitig die Verfügbarkeit des Systems erhöht \cite{Smirnov2025}. 

Eine weitere Ausbaustufe wäre die Trennung einzelner Komponenten. So könnten beispielsweise die Datenbank, die Speicherung der Videodateien und die Anwendungslogik auf separaten Systemen betrieben werden. Insbesondere bei einer großen Anzahl von Videos 
bietet sich langfristig die Verwendung eines Object-Storage-Systems an, da Speicher- und Rechenressourcen unabhängig voneinander erweitert werden können. Dadurch wird verhindert, dass die Speicherkapazität eines einzelnen Servers zum begrenzenden Faktor wird. 

Für einen internationalen Einsatz könnten zusätzlich mehrere Serverstandorte in verschiedenen Regionen betrieben werden. Nutzer würden dann automatisch mit dem geografisch nächstgelegenen Rechenzentrum verbunden werden, wodurch sich Latenzzeiten reduzieren lassen. 
Da die Anwendungsfälle dieser Arbeit jedoch auf einen regional begrenzten Einsatz ausgelegt sind, würde eine solche Infrastruktur für den aktuellen Prototyp einen unverhältnismäßig hohen zusätzlichen Aufwand verursachen. 

Insgesamt wurde die Architektur bewusst einfach gehalten, um die Kernfunktionalität der Videoübertragung, Speicherung und Bereitstellung zu evaluieren. Gleichzeitig erlaubt die gewählte Serverstruktur eine spätere Erweiterung hin zu einer verteilten und hochverfügbaren Infrastruktur, 
falls ein produktiver Einsatz höhere Leistungs- und Skalierungsanforderungen mit sich bringt.

\section{Datenschutz und Zugriffskontrolle} 

Da durch die Videofunktion des HB10 Bilddaten aus dem Spielumfeld aufgezeichnet, gespeichert und über eine Webplattform bereitgestellt werden, entstehen zusätzliche Anforderungen an Datenschutz und Zugriffskontrolle. Die aufgenommenen Videos können personenbezogene Daten enthalten, 
sobald eine natürliche Person anhand des Bildmaterials direkt oder indirekt erkennbar ist. Dies betrifft nicht nur den Spieler selbst, sondern auch Personen, die sich während der Aufnahme im Hintergrund befinden. Für das System ist deshalb zu unterscheiden, 
welche Personen für den Zweck der Aufnahme relevant sind und welche Personen lediglich unbeabsichtigt erfasst werden. 

Der Zweck der Videoaufnahme besteht darin, ein asynchron durchgeführtes Spiel nachträglich überprüfen zu können. Für diesen Zweck ist vor allem der Spieler relevant, dessen Würfe und Verhalten während des Spiels nachvollziehbar sein müssen. 
Die Aufnahme des Spielers ist daher funktional mit dem Spielmodus verbunden. 

\subsection{Datenminimierung und Hintergrundausblendung} \label{sec:blurr}

Personen im Hintergrund sind für diesen Zweck dagegen grundsätzlich nicht erforderlich. Aus diesem Grund sollte der Bildausschnitt soweit wie möglich auf den Spieler und die Dartscheibe begrenzt werden. Bereiche, 
die für die spätere Überprüfung nicht benötigt werden, sollten bereits vor der Speicherung entfernt oder unkenntlich gemacht werden (s. \cref{fig:background}). 

\begin{figure}
    \centering
    \includesvg[width=\textwidth]{img/background.svg}
    \caption[Ausblendung nicht benötigter Hintergrundbereiche]{Schematische Darstellung der möglichen Datenminimierung innerhalb einer Videoaufnahme. Der für die Spielüberprüfung relevante Bildbereich enthält den aufgenommenen Spieler. Nicht benötigte Bereiche außerhalb dieses Ausschnitts können vollständig entfernt werden. 
    Verbleibende Hintergrundbereiche innerhalb des Bildausschnitts können zusätzlich verpixelt oder unscharf dargestellt werden, damit dort erfasste unbeteiligte Personen nicht erkennbar bleiben.}
    \label{fig:background}
\end{figure}

Eine weitere Möglichkeit zur Reduzierung der Datenschutzrisiken besteht darin, nicht nur einzelne Personen im Hintergrund unkenntlich zu machen, sondern den gesamten nicht benötigten Hintergrund der Aufnahme auszublenden. In diesem Fall würde das Videobild so verarbeitet, 
dass nur der Spieler und die für die Spielüberprüfung notwendigen Bildbereiche sichtbar bleiben. Personen, die sich zufällig im Umfeld des Geräts aufhalten, wären dadurch nicht mehr erkennbar und würden nicht Bestandteil der später bereitgestellten Aufnahme werden. 
Gegenüber einer nachträglichen Erkennung und Verpixelung einzelner Personen hätte dieses Vorgehen den Vorteil, dass keine zuverlässige Unterscheidung zwischen verschiedenen Hintergrundpersonen erforderlich wäre. Stattdessen würde grundsätzlich nur der Bildbereich erhalten bleiben, 
der für den Zweck der Aufnahme benötigt wird. 

Durch eine solche Hintergrundausblendung kann das Risiko reduziert werden, dass personenbezogene Daten unbeteiligter Personen gespeichert oder veröffentlicht werden. Die Aufnahme enthält dann im Wesentlichen nur noch den Spieler, dessen Einwilligung vor Beginn des Spiels eingeholt wurde. 
Dadurch wird die datenschutzrechtliche Bewertung vereinfacht, da die Verarbeitung stärker auf die Person beschränkt wird, die aktiv am videopflichtigen Spielmodus teilnimmt. Voraussetzung dafür ist jedoch, 
dass die Ausblendung zuverlässig funktioniert und keine erkennbaren Personen oder sonstigen personenbezogenen Informationen im Hintergrund sichtbar bleiben. 

Auch bei einer vollständigen Ausblendung des Hintergrunds sollte jedoch berücksichtigt werden, dass das Video weiterhin personenbezogene Daten des Spielers enthält. Die Einwilligung des Spielers in die Aufnahme bedeutet daher nicht automatisch, 
dass das Video ohne Einschränkung für alle Personen veröffentlicht werden darf. Soll das Video nicht nur zur späteren Spielüberprüfung genutzt, sondern einem unbegrenzten Nutzerkreis zugänglich gemacht werden, müsste dieser weitergehende Zweck vorab klar beschrieben und gesondert bestätigt werden. 
Für den in dieser Arbeit betrachteten Anwendungsfall ist eine öffentliche Veröffentlichung jedoch nicht erforderlich. Der Zweck der Videofunktion besteht darin, ein asynchron durchgeführtes Spiel nachträglich überprüfen zu können. Dafür genügt es, 
die Aufnahme einem beschränkten Kreis berechtigter Nutzer innerhalb des jeweiligen Spiel- oder Turnierkontexts bereitzustellen. 

Die technische Umsetzung sollte daher beide Ansätze kombinieren. Einerseits sollte der sichtbare Bildbereich auf den Spieler und die spielrelevanten Inhalte begrenzt werden, indem der Hintergrund ausgeblendet oder stark unkenntlich gemacht wird. 
Andererseits sollte der Zugriff auf die gespeicherten Videos weiterhin serverseitig kontrolliert werden. Dadurch wird verhindert, dass auch solche Videos, die nur den Spieler zeigen, unbeabsichtigt oder zweckwidrig abgerufen werden können. 
Die Hintergrundausblendung reduziert somit die Menge der verarbeiteten personenbezogenen Daten, ersetzt aber nicht die Notwendigkeit einer Berechtigungsprüfung beim Abruf der Videos.

\subsection{Einwilligung und Speicherdauer}

Vor Beginn eines Spiels muss der Spieler darüber informiert werden, dass der gewählte Spielmodus eine Videoaufnahme erfordert. Dazu gehören der Zweck der Aufnahme, die spätere Bereitstellung über die Webplattform, die möglichen Empfänger der Aufnahme und die grundlegende Speicherdauer. 
Der Spieler sollte den Spielmodus erst starten können, nachdem er der Aufnahme ausdrücklich zugestimmt hat. Ohne diese Zustimmung darf kein Spiel in einem videopflichtigen Modus gestartet werden. Technisch kann dies dadurch umgesetzt werden, 
dass vor dem Spielstart ein Hinweisdialog angezeigt wird und der Start des Spiels erst nach aktiver Bestätigung freigegeben wird. Die erteilte Zustimmung sollte zusammen mit den Videometadaten gespeichert werden, damit später nachvollziehbar ist, auf welcher Grundlage die Aufnahme erstellt wurde. 
Für Spielmodi ohne Videoerfordernis kann das Gerät dagegen weiterhin ohne Aufnahme verwendet werden. 

Für die Speicherung der Videos sollte zusätzlich eine Begrenzung der Aufbewahrungsdauer vorgesehen werden. Da die Aufnahmen nur zur nachträglichen Überprüfung eines Spiels benötigt werden, sollten sie nicht unbegrenzt gespeichert bleiben. Eine mögliche Umsetzung besteht darin, 
eine feste Speicherdauer abhängig vom jeweiligen Turnier oder Wettbewerb zu definieren. Nach Ablauf dieser Frist können die Videodateien und die zugehörigen Metadaten gelöscht oder anonymisiert werden, sofern keine weitere Aufbewahrung erforderlich ist. Dadurch wird verhindert, 
dass sich über die Zeit unnötig große Mengen personenbezogener Videodaten ansammeln. Die Video-Guidelines des Europäischen Datenschutzausschusses betonen ebenfalls, 
dass bei Videoverarbeitung sowohl der Zweck als auch die Speicherdauer und der Zugriff auf die Aufnahmen klar begrenzt werden müssen \cite{edpb2019video}. 

\subsection{Technische Zugriffskontrolle und Schutzmaßnahmen}

Neben der Einwilligung ist auch die Zugriffskontrolle ein wichtiger Bestandteil des Datenschutzkonzepts. Die gespeicherten Videos dürfen nicht öffentlich abrufbar sein, sondern nur berechtigten Nutzern zur Verfügung stehen. Für den Prototypen kann hierfür zwischen mehreren Rollen unterschieden werden. 
Der HB10 benötigt eine technische Berechtigung, um Videos an den Server hochzuladen. Spieler dürfen die eigenen Videos abrufen, sofern sie einem Spiel eindeutig zugeordnet sind. Weitere Nutzer dürfen nur dann Zugriff erhalten, wenn dies für den jeweiligen Wettbewerb vorgesehen ist, 
beispielsweise zur Überprüfung eines Turniers. Administratoren benötigen erweiterte Rechte zur Verwaltung des Systems, sollten gespeicherte Videos jedoch ebenfalls nur im notwendigen Umfang einsehen können. 

Die Zugriffskontrolle sollte serverseitig umgesetzt werden. Die Webplattform darf also nicht nur einzelne Schaltflächen ausblenden, sondern der Server muss bei jeder Anfrage prüfen, ob der anfragende Nutzer für das jeweilige Video berechtigt ist. 
Dies betrifft sowohl die Metadatenabfrage als auch den Zugriff auf die HLS-Playlist und die einzelnen Videosegmente. Andernfalls könnte ein Nutzer durch direkte Kenntnis eines Dateipfads auf Videos zugreifen, obwohl diese nicht für ihn vorgesehen sind. 
Für die vorhandenen Endpunkte bedeutet dies, dass insbesondere der Abruf über \texttt{GET /video/playerId/filename} und die Liste über\texttt{GET /video/videos} nicht ohne Authentifizierung und Berechtigungsprüfung bereitgestellt werden sollten. 
Die Dateipfade der Videos sollten außerdem nicht direkt aus Benutzereingaben gebildet werden, sondern über die in der Datenbank gespeicherten Metadaten aufgelöst werden. Dadurch kann der Server kontrollieren, welches Video zu welchem Spieler, Turnier und Zugriffsrecht gehört. 

Auch die technische Absicherung der Verarbeitung ist zu berücksichtigen. Die Übertragung der Videodaten zwischen HB10, Server und Webplattform sollte verschlüsselt erfolgen. Zusätzlich sollten Zugriffe protokolliert werden, 
damit nachvollziehbar bleibt, wann ein Video hochgeladen oder abgerufen wurde. Solche Protokolle sind besonders dann hilfreich, wenn später geprüft werden muss, ob ein Zugriff berechtigt war.
Die gespeicherten Videodaten und Metadaten sollten außerdem durch geeignete technische und organisatorische Maßnahmen geschützt werden. Dazu gehören unter anderem beschränkte Benutzerrechte auf dem Server, getrennte Berechtigungen für Anwendung, 
Datenbank und Dateisystem sowie eine regelmäßige Überprüfung der Zugriffsmöglichkeiten. Die DSGVO nennt in diesem Zusammenhang unter anderem die Vertraulichkeit, Integrität, Verfügbarkeit und Belastbarkeit der Systeme sowie die regelmäßige Bewertung der getroffenen Maßnahmen \cite{eu2016gdpr}. 

Für das Datenmodell ergibt sich daraus, dass die reine Speicherung von Dateiname, Pfad und Spielerzuordnung langfristig nicht ausreicht. Für eine produktive Umsetzung sollten daher zusätzliche Informationen aufgenommen werden. Dazu zählen: 

\begin{itemize} 
    \item der Status und der Zeitpunkt der Einwilligung, 
    \item die Zuordnung zu einem Spiel oder Turnier, 
    \item eine Sichtbarkeitsstufe und 
    \item ein Ablaufdatum für die Löschung. 
\end{itemize} 

Dadurch kann der Server nicht nur erkennen, wo ein Video gespeichert ist, 
sondern auch, ob es überhaupt bereitgestellt werden darf und welche Nutzer darauf zugreifen können. Der Prototyp kann diese Anforderungen zunächst vereinfacht abbilden, sollte die Trennung von Videodatei, Metadaten und Berechtigungsinformationen jedoch bereits in der Architektur berücksichtigen. 

Insgesamt muss die Videofunktion so gestaltet werden, dass nur die für die Spielüberprüfung notwendigen Inhalte erfasst und bereitgestellt werden. Der Spieler kann im Rahmen des videopflichtigen Spielmodus aufgenommen werden, nachdem er vor Spielbeginn aktiv zugestimmt hat. 
Personen im Hintergrund sind dagegen für den Zweck der Aufnahme nicht relevant und sollten möglichst nicht erfasst oder nachträglich unkenntlich gemacht werden. Zusammen mit einer serverseitigen Zugriffskontrolle, verschlüsselter Übertragung, 
begrenzter Speicherdauer und nachvollziehbarer Protokollierung entsteht dadurch ein Datenschutzkonzept, das zur bestehenden Serverarchitektur passt und die spätere produktive Erweiterung vorbereitet.

\section{Reverse Proxy}

Es besteht auch die Möglichkeit mehrere Instanzen des Servers gleichzeitig laufen zu lassen, und zwischen Server und Client einen Reverse Proxy\cite{nginx_reverse_proxy} zu platzieren, welcher den Client (HB10 oder ein Nutzer) zu einem passenden Server weiterleitet. Für den Prototypen wurde entschieden dies auszulassen,
da es bei der zu erwartenden Menge an Nutzern nicht notwendig ist, mehrere Server aus Gründen des load balancing aufzusetzen. Da der HB10 gegenwärtig überwiegend in Deutschland eingesetzt wird, besteht derzeit keine Notwendigkeit, regionsspezifische Serverinfrastrukturen bereitzustellen, 
um diese räumlich näher an den Clients zu positionieren.

\newpage